# UX/UI Review & Recommendations: Employee Evaluation System

จากการสำรวจ Codebase ปัจจุบัน (`src/app/evaluations/page.tsx`, `layout.tsx`, `globals.css`) ผมพบว่าระบบทำงานได้ถูกต้องตาม Logic แต่ในมุมของ User Experience (UX) และ User Interface (UI) ยังสามารถปรับปรุงให้ดู **"พรีเมียม"**, **"ทันสมัย (Modern)"**, และ **"ใช้งานง่าย (User Friendly)"** ได้มากขึ้นครับ

นี่คือสรุปสิ่งที่พบและข้อแนะนำครับ:

---

## 1. Visual Design & Theme (รูปลักษณ์และความสวยงาม)

### 🔴 สิ่งที่พบ (Current State)
*   **Color Palette:** ใช้โทนสี "Earth Tone" หนักๆ (น้ำตาลเข้ม `#5d4037`, ส้ม `#ff5722`) ตัดกับพื้นขาว/เทา
    *   *ปัญหา:* สีน้ำตาลเข้มที่ใช้เป็น Background ของ Header การ์ดทำให้หน้าจอดู "หนัก" และ "ทึบ" เหมือนโปรแกรมยุคเก่า สีส้มที่ใช้เป็นสีหลักมีความสดมาก อาจทำให้รู้สึกล้าสายตาเมื่อใช้นานๆ
*   **Typography:** มีการ import `Geist` font มา แต่ใน `globals.css` กลับ override เป็น `Arial, Helvetica` ทำให้ฟอนต์ดูธรรมดาและไม่ clean
*   **Spacing & Layout:** การจัดหน้ายังดูแน่น (Dense) โดยเฉพาะหน้าประเมินที่มีข้อมูลเยอะ การเว้นระยะห่าง (Padding/Margin) ยังไม่สมดุล

### ✨ ข้อแนะนำ (Recommendations)
*   **Modern Color System:** ปรับลดการใช้น้ำตาลเข้มเป็นสีพื้นหลังก้อนใหญ่ๆ ให้เปลี่ยนไปใช้ **"สีขาว"** หรือ **"เทาอ่อน (Surface Colors)"** เป็นหลัก แล้วใช้สีน้ำตาล/ส้ม เป็นเพียง **Accent Color** (เช่น เส้นขอบ, ปุ่ม, หรือ Icon) เพื่อให้หน้าจอดูสว่างและสะอาดตาขึ้น
*   **Typography:** บังคับใช้ฟอนต์ `Geist` (หรือฟอนต์ไทยสวยๆ เช่น `IBM Plex Sans Thai` หรือ `Sarabun`) ให้ทั่วถึงทั้งแอพ เพื่อให้ดูเป็นระเบียบ
*   **Glassmorphism & Shadows:** เพิ่มมิติให้ UI ด้วยเงาที่นุ่มนวล (Soft Shadows) แทนเงาแบบแข็งๆ และอาจใช้ Effect กระจก (Blur) ในส่วนของ Navbar หรือ Sticky Menu

---

## 2. Interactive Components (การใช้งานปุ่มและฟอร์ม)

### 🔴 สิ่งที่พบ (Current State)
*   **Browser Alerts:** การแจ้งเตือนต่างๆ (บันทึกสำเร็จ, Error) ใช้ `alert()` ของ Browser ซึ่งเป็นการขัดจังหวะการทำงาน (Blocking) และดูไม่เป็นมืออาชีพ
*   **Native Selects:** ใช้ `<select>` ธรรมดาของ HTML ซึ่งหน้าตาจะเปลี่ยนไปตาม Browser/OS ควบคุมความสวยงามยาก
*   **Buttons:** ปุ่มให้คะแนน 1-5 เป็นวงกลมใหญ่ๆ ที่กินพื้นที่มาก และปุ่ม Action ต่างๆ ยังดูแข็ง

### ✨ ข้อแนะนำ (Recommendations)
*   **Validations & Toasts:** ยกเลิกการใช้ `alert()` และเปลี่ยนมาใช้ **"Toast Notification"** (แถบแจ้งเตือนเล็กๆ มุมจอ เช่น `sonner` หรือ `react-hot-toast`) ที่เด้งขึ้นมาสวยงามและหายไปเอง
*   **Custom Select/Dropdown:** ใช้ Library UI เช่น `Radix UI` หรือ `Headless UI` เพื่อทำ Select Box ที่สวยงาม, Search ได้, และหน้าตาเหมือนกันทุก Browser
*   **Smart Input Controls:**
    *   เปลี่ยนปุ่มวงกลม 1-5 เป็น **Segmented Control** หรือ **Slider** หรือ **Rating Stars** เพื่อประหยัดพื้นที่และเข้าใจง่าย
    *   เพิ่ม **Micro-interactions** เช่น เมื่อเอาเมาส์ไปชี้ ปุ่มควรจะขยับนิดหน่อย หรือเปลี่ยนสีอย่างนุ่มนวล (Smooth Transition)

---

## 3. Data Presentation (การนำเสนอข้อมูล)

### 🔴 สิ่งที่พบ (Current State)
*   **Long Scrolling Form:** หน้าประเมิน (`evaluations/page.tsx`) ยาวมาก ผู้ใช้ต้องเลื่อนขึ้นลงไกลเพื่อดูข้อมูลพนักงานและกดให้คะแนน
*   **Cards:** ข้อมูลสถิติ (มาสาย/ขาด/ลา) แสดงเป็น Box สี่เหลี่ยมธรรมดา แยกความแตกต่างลำบาก

### ✨ ข้อแนะนำ (Recommendations)
*   **Sticky Employee Header:** ตรึงข้อมูลพนักงาน (ชื่อ, ตำแหน่ง, รูป) ไว้ด้านบนสุดหรือด้านข้างเสมอ (Sticky) เพื่อให้ผู้ประเมินเห็นหน้าและชื่อคนที่จะประเมินตลอดเวลา ไม่ต้องเลื่อนกลับไปดู
*   **Visual Stats:** ใช้ **Graph** หรือ **Progress Bar** เล็กๆ แสดงสถิติการมาทำงาน แทนตัวเลขแห้งๆ (เช่น หลอดพลังงานสีเขียว/แดง แสดงวันลาที่เหลืออยู่)
*   **Collapsible Sections:** หัวข้อการประเมิน (Categories) ควรสามารถ "ย่อ/ขยาย" ได้ เพื่อให้โฟกัสทีละเรื่อง

---

## 4. Technical & Code Structure (โครงสร้างโค้ด)

### 🔴 สิ่งที่พบ (Current State)
*   **Giant File:** `src/app/evaluations/page.tsx` มีขนาดเกือบ 800 บรรทัด รวมทั้ง Logic, UI, และ CSS styles ไว้ด้วยกัน ทำให้แก้ยาก
*   **No Reusable Components:** ไม่มีการแยก Button, Card, หรือ Input ออกมาเป็น Component กลาง (เช่น `<Button>`, `<EvaluationCard>`) ทำให้ถ้าจะแก้ดีไซน์ ต้องตามแก้ทุกจุด

### ✨ ข้อแนะนำ (Recommendations)
*   **Component Refactoring:**
    *   สร้าง Folder `src/components/ui`
    *   แยกชิ้นส่วน UI ออกมา เช่น `Button.tsx`, `ScoreSelector.tsx`, `EmployeeProfileCard.tsx`
*   **Data Separation:** แยก Logic การคำนวณ (Scoring Logic) ไปไว้ใน Custom Hook (เช่น `useEvaluationScoring.ts`) เพื่อให้ไฟล์หน้าจอ UI สะอาดตา

---

## 📅 Proposed Action Plan (แผนการปรับปรุง)

หากคุณเห็นด้วย ผมขอเสนอให้เริ่มดำเนินการดังนี้ครับ:

1.  **Refactor Structure:** แยก Component ย่อยออกมาจากหน้า `page.tsx` (โดยยังไม่แก้ดีไซน์) เพื่อให้โค้ดจัดการง่าย
2.  **Theme Overhaul:** ติดตั้ง Font ใหม่, ปรับ `tailwind.config`, และแก้ `layout.tsx` ให้ดู Modern ขึ้น
3.  **Enhance Experience:** เปลี่ยน `alert()` เป็น Toast, ปรับหน้าตา Input/Select
4.  **Polish UI:** ปรับ Styling ของ Card และปุ่มต่างๆ ให้สวยงาม ใส่ Animation

**คุณอยากให้ผมเริ่มที่ข้อ **1 (Refactor)** เพื่อเตรียมโครงสร้าง หรือ **2 (Theme)** เพื่อให้เห็นความสวยงามก่อนดีครับ?**
