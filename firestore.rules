rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 🔒 SECURE: Only allow access if user is authenticated via Firebase Auth
      allow read, write: if request.auth != null;
    }
  }
}
